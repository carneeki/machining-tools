env = Environment()
env.Replace(CC='cc')
env.Replace(CXX='c++')
env.Replace(CCFLAGS='-g')
env.Replace(LINKFLAGS='-lm')
env.Program('divide',['divide.c'])
env.Program('mandrel',['mandrel.c'])
env.Program('sine',['sine.c'])
Install("out")
